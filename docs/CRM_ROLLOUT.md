# ADA Calendar / CRM release checklist

Release procedure and verification record for the paired integration branches. Both applications, integration schemas and all 36 current CRM client matches are installed. Calendar has 47 clients after the 17 approved additions. Calendar booking, CRM assignment checks and synchronization are active. The approved live pilot passed booking, conflict/alternative previews and owner cancellation with synchronization back to CRM. The test booking is cancelled and consumes no time. Bryan authorized normal team email delivery on September 15; the Calendar notification worker is now verified in live mode.

## Requester cancellation release — September 23, 2026

- Added Calendar API `GET/POST /api/integrations/crm/v1/cancellations` and migration `202609230001_crm_task_cancellation.sql`; CRM adds `deploy/calendar-cancellation.sql`, the gateway/recovery path, and a **Cancel task…** confirmation dialog. Remove × remains available for ordinary CRM tasks; linked tasks use cancellation instead. No task is physically deleted.
- Both servers and SQL bind the action to the original verified requester and exact operation intent. Calendar checks a fresh review token, locks the workspace/connection, preserves unrelated work and completed history, and uses the shared scheduler/transaction for the authorized single-task cancellation. Protected time and completed tasks are not cancellable by requesters. Pending requests are recorded as requester withdrawals, not owner rejections, with a cancelled public projection. The verified account, reason and timestamp remain in source history and Calendar activity/request history.
- Existing operation lookup, atomic settlement and the ordered sync feed recover lost responses without duplicate cancellation or retaining human tokens. CRM only confirms after the Calendar result is durably applied. Kyle's email preference is respected. No new credential or feature flag is needed.
- Validation: all 1,385 Calendar tests, Calendar typecheck/lint/production build, all 78 CRM tests and generated build/syntax checks pass. Both local SQL suites pass, including new cancellation authorization, stale review, acknowledgement, completed-history retention, protected work, email preference, immutable retry and confirmation tests. Full-stack real local browsers/Auth/SQL pass new cancellation UI at desktop/mobile sizes, audit in both systems, lost acknowledgement/restart recovery and pending withdrawal, plus all existing booking/approval/Undo/sync/revocation checks. Only fictional local accounts/tasks and queued/captured mail were used.
- Rollout order: Calendar migration and application first; CRM database update then application. Verify source/deployment identities, health, unchanged email opt-out and continued enforcement/sync. Rollback applications CRM first if needed; retain the additive database/audit history and drain ambiguous operations before retiring the API. Do not reset enforcement or delete source/audit records.

## Kyle’s Calendar emails disabled — September 16, 2026

- Bryan explicitly requested that only Kyle stop receiving Calendar added/completed/workload emails while retaining CRM assignment checks and completion synchronization. Kyle’s active requester membership (`kyle@alphadogagency.com`) now has `receive_updates=false`; no other membership or email preference was changed. This also suppresses Calendar draft updates and weekly summaries to Kyle. Authentication emails and CRM’s separate mail settings are outside this change.
- Applied Calendar migration `202609160001_requester_email_preferences.sql`. The trusted notification enqueue function now skips opted-out requesters instead of rejecting their otherwise valid scheduling transaction. CRM task notifications honor the same preference **after** recording the change feed. Queue claims recheck the preference and archive any pre-existing opted-out messages as captured. Owner review alerts, other recipients, source authorization and booking rules remain unchanged.
- Local database regressions pass ordinary update/draft/weekly suppression, queued-message capture, unchanged other recipients and owner alerts, invalid-recipient rejection and preserved requester access. The integration transaction suite verifies owner completion commits successfully with an opted-out CRM requester and still records the completed CRM projection without sending that requester mail. All fixture data rolls back; no real test task or email was created.
- Lint, type checking, all 1,385 unit tests and production build pass. The sandboxed build stalled; the build succeeded outside the sandbox. The four isolated database suites pass. No UI/application/CRM code changed, so no browser workflow or Railway application deployment is required for this database-only fix.
- Before opt-out, there were no queued emails. Existing history was 67 captured and five delivered for Kyle, three delivered for Bryan; historical messages were not replayed. Calendar version 116 and change sequence 2 were recorded before the preference update. CRM enforcement/acceptance remained enabled with a healthy sync cursor 2 and no error.
- To re-enable Kyle’s Calendar emails later, set this same membership’s `receive_updates=true`; do not requeue captured history. Keep this migration in place. If rolling back the database functions, restore preferences first so the old validation does not reject calendar commits. The worker stays in live mode for other recipients.

## CRM Calendar shortcut released — September 15, 2026

- Bryan requested a **Bryan’s Calendar** link as soon as a teammate chooses him for a CRM task. CRM source `ade72f9d27dbf3780d2e5b8142ac9d38b4ef4a44` adds the link beneath the new-task form when Bryan is selected and in the Calendar scheduling dialog for new assignments and reassignments. It opens a separate tab with `noopener noreferrer` and explains that Calendar sign-in is required. Membership rules are unchanged.
- The public link comes from the existing `ADA_CALENDAR_URL`, validated as a credential-free HTTPS origin. `/config.js` now exposes that public origin as `calendarUrl` alongside the existing two public Supabase fields; the Calendar credential remains server-only. No environment variable or schema update was needed.
- Browser verification exposed an existing automatic-refresh issue that cleared unfinished task forms. Unsaved title, due date and owner now remain in memory across row refreshes and panel reopening, and clear after successful submission. They are not persisted to browser storage or the database before submission.
- `npm run check` passes the generated builds and server/browser syntax checks; all 78 existing tests pass, including extended public-config credential isolation and draft persistence/reset checks. Local fictional browser verification covers immediate link visibility, hiding for another assignee, new/reassignment dialogs, desktop/mobile layout, preserved draft after rerender, and successful booking clearing the form. No real test task or email was created. The isolated browser fixture was stopped.
- Pushed the feature branch `codex/bryan-calendar-link`, then fast-forwarded CRM main from `5dabe28` to `ade72f9`. Railway deployment `525c90b0-653b-4a83-a11d-698933c11cad` succeeded; GitHub deployment `6466307444` records success at 19:03:11 UTC. The Railway origin returns healthy status and the correct public Calendar URL. Its HTML exactly matches the tested build, SHA-256 `dd3570fc0ca10d16175b301474926c329491f1203f41bbc48089fe603643c1d5`.
- The custom-domain command-line health check received HTTP 403; the signed-in Chrome page initially loaded, but native browser control subsequently stopped exposing its page content. A final signed-in production click-through is therefore not claimed. Release verification rests on the successful deployment, exact live artifact/configuration match, and local browser workflow checks.
- Post-release hosted verification confirms CRM enforcement and acceptance remain true, sync cursor 2, no sync error and a successful sync at 19:42:12 UTC. Calendar code, production tasks, client mappings and live email configuration were not changed. Existing CRM tabs should reload to use the new link.

## Normal Calendar email delivery enabled — September 15, 2026

- Bryan explicitly requested **Okay lets unrestrict emails**, confirming this is the ADA Calendar side. Changed only the existing Supabase notification worker's `EMAIL_MODE` from `test` to `live` on Calendar project `xlenubijjhhippntcvim` at 18:37:28 UTC (14:37 Indianapolis). No CRM email setting, Railway variable, credential, recipient preference, code or schema was changed.
- Before activation, read-only checks confirmed zero queued messages, the existing once-per-minute `ada-notification-worker` Cron job active, three Bryan notifications delivered and 67 Kyle notifications captured. Active member settings show Kyle as the requester receiving workload updates; Bryan's existing owner notification preferences remain unchanged. The sender/provider configuration remains the one already verified by the successful allowlisted pilot.
- Secret metadata independently verifies `EMAIL_MODE=live`. The Bryan-only `EMAIL_TEST_ALLOWLIST` remains configured for a possible return to test mode; live mode does not enforce it. Existing sender, provider, app URL and worker-authentication settings remain configured, with no secret values copied into these records.
- The next scheduled invocation at 18:38:00 UTC returned HTTP 200, no timeout, and `{"processed":0,"mode":"live"}` (pg_net response 11606). No manual worker invocation or extra test email was needed. All 67 captured messages remain captured, the three delivered messages remain delivered, and the queue remains empty. Captured test history was not replayed. This verifies live-mode activation and worker health, not a new post-activation delivery to Kyle.
- Normal future Calendar notifications now use the existing recipient rules, including weekly summaries for requesters who receive updates. To restore the prior restriction if needed, set this same worker's `EMAIL_MODE=test`; the retained Bryan-only allowlist will apply again. Do not requeue captured history. This checkpoint supersedes earlier restrictions and pending normal-delivery statements below.

## All current CRM clients matched — September 15, 2026

- Bryan approved creating and matching the 16 remaining listed CRM clients. A fresh read found Hoffman Orthodontics as a seventeenth addition; the first save was blocked by automatic approval review before execution because Hoffman was outside the original 16. Bryan then explicitly approved **Yes, include Hoffman Orthodontics**. No blocked mutation was treated as saved.
- Added all 17 through the owner-authenticated Calendar **Clients → Save directory** form, then confirmed each exact CRM ID and Calendar client through **CRM → Confirm client match**. The existing connection and its 19 previous mappings were preserved. No direct database writes substituted for owner authorization.
- Read-only hosted verification confirms 47 Calendar clients, with all original 30 IDs, names and aliases unchanged; all 17 additions appear once with new IDs and empty aliases. The 36 mappings cover every client in the refreshed CRM directory and all have the connection owner's `confirmed_by`. The Calendar UI visibly lists all 36 saved matches. Exact pairs are in `CRM_CLIENT_MATCH_PROPOSALS.json`; there are no unresolved current CRM clients.
- The Calendar booking API and connection remain visibly enabled. The change sequence remains 2 from the cancelled live pilot; this client setup created no task or reserved work session. No credentials, hosting settings, email configuration, source code or database schema changed, and no deployment or GitHub push was required. Normal team email delivery remains unapproved and restricted as recorded below.
- These additions resolve the unmapped-client limitation recorded in the earlier pilot checkpoint. Any CRM client created later still requires an owner-confirmed Calendar match.

## Live pilot passed; integration active — September 15, 2026

- Bryan brought the setup window forward and confirmed **Ready**. The already approved pilot was completed through the real signed-in CRM and Calendar owner interfaces. No further credential rotation, source-code change, migration or GitHub push was needed.
- Calendar deployment `cd041a00-6fdd-4e1a-8415-dd2b63feb693` reports **ACTIVE / Deployment successful**. After refreshing the owner settings, the UI explicitly confirms the booking API and connection are enabled. Its source remains the released `1a75bb30313a0f812e69b3021ecbe9c8f9fa2bd8`; `ADA_CRM_INTEGRATION_ENABLED=true` and `ADA_CRM_BOOKING_ENABLED=true`.
- Saved and deployed CRM's `ADA_CALENDAR_ENABLED=true` and `ADA_CALENDAR_SYNC_ENABLED=true`. Deployment `be73f002-195e-494f-9f91-f07d5911657b` reports **Active / Deployment successful**, with source `5dabe28092d78b0ac1b39155962d6c36eb44446e`. Its build waited several minutes before starting; no duplicate deployment was created. Both CRM health origins and Calendar health return 200, and the CRM origin page still matches the tested artifact SHA-256.
- Applied the explicitly approved CRM database enforcement with acceptance initially false. The signed-in CRM visibly paused assignments. The new sync worker then pinned connection `e154f3c1-35e0-4cb7-9387-b31d14435a4f`, caught up without error and released its lease. Only after that verification was `accepting=true` applied, guarded by the correct connection identity and a recent successful sync. Enforcement and acceptance are now true; syncing stays enabled.
- **Clean-fit booking:** signed in to CRM as `bryan@alphadogagency.com`, used the **Alpha Dog Agency** internal company (`internal-alpha-dog-agency`, mapped Calendar client `90e9cebe-3334-46de-8ae5-650d89900dbf`), and submitted **CRM connection test**, Software, 0.5 hours, exact start **September 17, 13:30 America/Indiana/Indianapolis**. Calendar's human-authenticated preview reported **This task fits**, with exactly 13:30–14:00 and no displaced work. After **Book task**, the CRM showed **ADA Calendar · Booked**, and Calendar's owner view showed one matching 0.5-hour session. Independent hosted queries confirmed exactly one linked task and the same session on both sides.
- Test source task: `mu2yo0h2b7w4j`; CRM submission/Calendar operation: `a37c5fda-75c4-4f62-8f64-fd2d18dbebf4`; Calendar work item: `538a1b3c-8d17-4daf-8523-36d2e5ef6122`. Booking was committed at 17:46:05 UTC with change sequence 1.
- **Conflict and alternative:** a second, unsubmitted **CRM connection test - conflict preview** for the same occupied half-hour reported **Bryan’s approval is required**. It named the original test work, displayed the proposed displacement and offered three other openings. No direct **Book task** action was available for that conflict. Reviewing the September 17, 14:00–14:30 alternative returned **This task fits**. Closed that review without booking or sending an approval request. Its submission `daf4698d-fd0c-4e38-80e9-39080e0a10c1` remains an unsubmitted `review` for routine expiry/cleanup; it is not a task, pending owner request or reservation. Actual owner approval of a submitted conflict remains covered by the existing local suite, not by this limited live pilot.
- **Cancellation and sync:** used **Cancel this work** in Calendar's owner view at 17:50:22 UTC. Event `4daebcd2-bb8b-4efa-a966-b1133d82bb07` cancelled only the test work. Calendar displayed **Cancelled**, **Work sessions 0** and **No hours booked**; CRM independently updated to **ADA Calendar · Cancelled** with no booked time. Hosted checks confirm zero planned sessions for the test, one retained cancelled source task, empty projected sessions and sequence/cursor 2. At 17:51:27 UTC, sync was healthy, with no error or active lease. Retain the cancelled test/source history; do not delete integration links or reset the cursor.
- **Notifications:** both authorized Bryan notifications (booking and cancellation) reached **delivered**, each after one attempt and with no delivery error. Kyle's cancellation update reached **captured**, with the explicit reason that he is outside the test allowlist and no provider ID. CRM queued zero duplicate assignment emails for the linked task. The existing notification worker remains `EMAIL_MODE=test`, `EMAIL_TEST_ALLOWLIST=bryan@alphadogagency.com`; no normal team-email activation or manual notification-worker invocation occurred.
- The scheduling integration is active for mapped clients. The 16 unresolved client names remain unmapped. Normal team email delivery still needs Bryan's explicit authorization; do not silently broaden the test allowlist. Existing local verification covers the broader owner approval, protected-time, reply, completion/Undo and recovery scenarios. This live checkpoint proves the specific booking/conflict/alternative/cancellation/sync/email pilot described above.

## Controlled live pilot authorized — September 15, 2026

- Bryan explicitly approved activating Calendar checks for new CRM assignments to him and testing **CRM connection test**, under **Alpha Dog Agency**, on **September 17, 2026, 13:30–14:00 America/Indiana/Indianapolis**. The agreed checks are booking in both systems, conflict warnings, synchronization and cancellation of the test task afterward. Calendar must revalidate the slot through its normal preview before booking; the read-only planning snapshot found it unbooked. September 16 is fully booked.
- Test email delivery is authorized only to **bryan@alphadogagency.com**. Supabase secret metadata hashes independently match `EMAIL_MODE=test` and `EMAIL_TEST_ALLOWLIST=bryan@alphadogagency.com` for the existing notification worker. Kyle's generated test updates must remain captured, not delivered. Do not broaden normal delivery or run a separate notification worker.
- Bryan was told and approved that Calendar checks remain required for new Bryan assignments after activation; acceptance can still be paused. Pre-activation hosted CRM baseline: enforcement/acceptance false, null connection identity, zero submissions, zero linked tasks and sync cursor 0. No production database activation or live test task has happened yet.
- Submitted the approved Calendar booking flag update (`ADA_CRM_BOOKING_ENABLED=true`) to Railway for deployment; final deployment verification remains pending. The existing Calendar API/connection is already enabled. CRM's first flag edit was interrupted before a saved value could be confirmed; inspect fresh UI state before proceeding. Chrome repeatedly changed to unrelated windows, so Bryan was asked to keep the integration window in front. Do not treat attempted UI actions as saved configuration.
- Next: finish and verify the Calendar deployment; enable CRM enforcement with acceptance paused, deploy its assignment/sync flags, verify healthy catch-up and connection pinning, then enable acceptance and use the real signed-in CRM form for the approved test. Preserve existing source commits and additive schemas. Cancel the test through Calendar's owner-authorized scheduler, verify CRM catches up and the work slot is freed, and record notification outcomes and source/task IDs.

## Replacement credential deployed; live service authentication passed — September 15, 2026

- Bryan generated a replacement key for the existing connection and submitted it in Railway himself. Read-only comparison of Calendar's new one-time textarea and Railway's saved variable editor confirms an exact match, 91 characters and the expected connection identity. No credential was printed or written to source files. The original copied key is obsolete; do not reuse it or rotate again unnecessarily.
- Deployed the single staged `ADA_CALENDAR_CREDENTIAL` update. Railway reports **Active / Deployment successful** for `3fcc73c6-d0d9-4aa6-839c-1be187fb4a8c`, using the same tested CRM source `5dabe28092d78b0ac1b39155962d6c36eb44446e`. The previous deployment is `f6c4d57e-16bf-4e80-9bc5-924233eed2bf`; its credential is obsolete after rotation. Any application rollback must retain the new credential. The new origin health and page return 200, and the page SHA-256 still matches the tested artifact.
- Railway's CRM Console connected to this deployment's instance `9bf09f3f-5faa-41be-9df2-00d097c44f1b`. A read-only probe using the deployed `calendar-integration.mjs` successfully authenticated to Calendar's change feed: API version 1, zero changes, cursor 0 and `hasMore=false`. The same probe verified that the human-authenticated status route rejects a missing user token with 401 `crm_sign_in_required` and that a deliberately invalid service key is rejected with 401 `crm_unauthorized`. Runtime CRM booking and sync flags are both false. The probe printed summary fields only, imported no notification worker and created no task.
- Independent public checks confirm Calendar and both CRM health origins return 200. Anonymous Calendar change-feed access returns 401 `crm_unauthorized`; anonymous CRM status returns 401 `sign_in_required`. Read-only hosted SQL confirms the Calendar connection remains enabled with 19 mappings and zero linked tasks. CRM enforcement and acceptance remain false, pinned connection null, sync cursor 0 and last sync success null. Calendar booking remains disabled on its unchanged deployment `ececea26-bc83-4e14-8254-b7522b7070fa`.
- Service authentication is now verified; this does not establish human-authenticated availability or a complete live scheduling flow. Agree on a disposable task, open slot, test participants and explicit notification recipients before the controlled activation window. Keep Calendar booking and CRM assignment/sync flags, enforcement and acceptance off until that step is authorized. No live test task or real test email was created by this verification.

## Calendar API enabled; copied credential mismatch identified — September 15, 2026

- Bryan answered **Yes please** to the explicit request to enable the Calendar API and existing CRM connection for authentication/availability checks, with booking and syncing off. This access approval is complete; do not request it again for the same scope.
- Deployed the two prepared Calendar variables. Railway reports **ACTIVE / Deployment successful** for `ececea26-bc83-4e14-8254-b7522b7070fa`, using the previously tested source `1a75bb30313a0f812e69b3021ecbe9c8f9fa2bd8`. Runtime owner setup now reports that CRM bookings are disabled without the earlier API-disabled notice. Anonymous integration status returns 401 `crm_unauthorized`; both applications' health endpoints return 200.
- Enabled existing connection `e154f3c1-35e0-4cb7-9387-b31d14435a4f` through Calendar's owner-authenticated **Enable connection** button. Read-only hosted SQL confirms `enabled=true`, all 19 mappings intact and zero linked tasks. No connection was duplicated, and no scheduling authority or owner identity was bypassed.
- Opened the existing CRM deployment's Railway **Console**, which connected successfully to instance `e72d88b4-a3a2-46f2-9e43-e3598dfb2858`. A read-only Node probe using the deployed `calendar-integration.mjs` confirms Node `v24.21.0`, the intended HTTPS origins/connection ID, booking and sync flags false, CRM enforcement/acceptance false and null pinned connection. It imported no notification worker and made no scheduling mutation.
- The authenticated change-feed probe did **not** pass. A direct diagnostic returned HTTP 401 `crm_unauthorized` / `Invalid CRM connection credential.` Comparing the earlier legitimate Calendar issuance UI observation with the saved Railway UI observation found two changed characters in the otherwise correctly formatted 91-character key. Neither value was written to these records. The planned missing-human and wrong-key checks did not run after the first probe threw; do not claim they passed. No real availability response has been verified yet.
- Prepared Railway's existing `ADA_CALENDAR_CREDENTIAL` **Edit** form without entering or submitting a new value. Prepared the Calendar owner CRM panel with **Replace connection key** available, connection enabled and booking disabled. Bryan must generate a replacement for this same connection, copy the complete new key directly from its textarea with keyboard copy, paste it into the prepared Railway field and submit. This follows the browser tool's credential-change hand-off requirement. Do not rotate through a direct SQL bypass, create another connection, reuse the copied mismatch or print the key.
- After Bryan submits, verify the saved value, deploy the single CRM variable change, then repeat the service-authentication probe and complete the negative checks. Keep CRM booking/sync flags, enforcement/acceptance and Calendar booking off. The API/access approval above remains valid; a real test task, open slot and notification recipients still need separate agreement before a live scheduling pilot.

## CRM connection configuration deployed — September 15, 2026

- Bryan corrected and submitted the original credential. Read-only inspection of its saved value confirms exactly 91 characters, zero line breaks, the required token format and connection ID `e154f3c1-35e0-4cb7-9387-b31d14435a4f`. No rotation was needed or performed, and no credential value was written to these records.
- Deployed the five staged CRM variables together. Railway reports **ACTIVE / Deployment successful** for `f6c4d57e-16bf-4e80-9bc5-924233eed2bf`, using the existing tested CRM source `5dabe28092d78b0ac1b39155962d6c36eb44446e`. CRM's `ADA_CALENDAR_ENABLED=false` and `ADA_CALENDAR_SYNC_ENABLED=false` are preserved; both HTTPS origins were independently verified. The previous active configuration deployment is `b186ae0d-e84a-4a2d-90c5-d16a8dfdc4d2`.
- Both CRM health origins return 200. The deployed origin HTML still matches the tested artifact hash. Anonymous `/api/calendar/status` returns 401 `sign_in_required`, and the server integration source returns 404. `/config.js` contains only the expected public Supabase URL and publishable key, with no Calendar credential. Reloaded the signed-in CRM and verified Clients, My Tasks (Bryan's five open tasks) and Resources (two entries) load. No task or resource was changed during these checks.
- Read-only hosted checks confirm the Calendar connection remains disabled with 19 mappings and zero linked tasks. CRM enforcement and acceptance remain false, connection identity is null, sync cursor is 0 and last sync success is null. Calendar health returns 200; its integration status still returns 503 `crm_disabled`, as expected before activation.
- In **ADA Calendar / production / ADA-Calendar**, staged `ADA_CRM_BOOKING_ENABLED=false` and `ADA_CRM_INTEGRATION_ENABLED=true`. Inspected both saved value editors to verify the exact booleans, then closed them without changes. Railway shows **Apply 2 changes** and 13 variables (the prior 11 plus these flags). These Calendar changes have **not** been deployed.
- Prepared Calendar's owner CRM panel with the existing disabled connection. Asked Bryan for action-time confirmation to deploy the prepared API setting and enable that connection for authentication/availability checks while booking and syncing stay off. This is the browser tool's security-sensitive access confirmation, not a request to recreate credentials or reapprove the 19 mappings. Await that response before the two activation actions. No controlled test task or real test email is authorized by this access-only confirmation.

## Original credential staged; copy correction pending — September 15, 2026

- Bryan clarified that he had saved the original one-time connection key. No replacement is needed and no rotation has been performed. This supersedes the replacement-key recommendation in the preceding checkpoint.
- Bryan added `ADA_CALENDAR_CREDENTIAL` himself. Railway shows **Apply 5 changes** and 12 service variables: the original seven and the five intended integration settings. Independently checked the two HTTPS origins and verified both `ADA_CALENDAR_ENABLED` and `ADA_CALENDAR_SYNC_ENABLED` are exactly `false`.
- Read-only inspection of the credential's value editor found one embedded newline: 92 characters instead of the expected 91. With that single whitespace character removed, the value has the expected format and the existing connection ID `e154f3c1-35e0-4cb7-9387-b31d14435a4f`. This is a format/identity check, not proof of successful service authentication. No private credential value is stored in these records.
- Deployment was withheld. The credential editor is open in the CRM Railway tab, with the caret positioned immediately after the unwanted newline. Bryan must press the Mac **Delete** key once and click **Submit**; the computer-use credential-change hand-off rule applies. No edit was submitted by the agent. Re-read the saved raw value to confirm its exact format before deploying all five staged variables together.
- Connection/API/booking flags and CRM enforcement/acceptance remain disabled. No live test task, manual notification-worker run or real test email was performed. After the corrected configuration deployment, verify the CRM starts successfully, then continue the paused connection checks and agreed live pilot.

## Client matches installed; CRM hosting prepared — September 15, 2026

- Browser control recovered. Applied all 19 approved pairs using Calendar's owner-authenticated **Confirm client match** form. The final saved UI includes every pair, including United Way and Z Roofing. A read-only hosted query confirms each exact CRM/Calendar ID pair in `CRM_CLIENT_MATCH_PROPOSALS.json`, all 19 `confirmed_by` values equal the connection's creating owner, and connection `e154f3c1-35e0-4cb7-9387-b31d14435a4f` remains disabled. No new clients were created; the 16 unresolved CRM names remain unmapped.
- The previous one-time key is no longer displayed after the settings panel was closed. Do not recover it from browser internals or create a new connection. Use **Replace connection key** on the existing connection and preserve its UUID and mappings. No replacement was generated at this checkpoint.
- In Railway's **ADA CRM / production / ada-crm** service, staged four new variables: `CRM_PUBLIC_ORIGIN=https://alphadogcrm.com`, `ADA_CALENDAR_URL=https://ada-calendar-production.up.railway.app`, `ADA_CALENDAR_ENABLED=false`, and `ADA_CALENDAR_SYNC_ENABLED=false`. Railway shows **Apply 4 changes** and 11 service variables (the original seven plus these four). No Deploy action was taken; runtime settings and the active deployment are unchanged.
- Prepared a fifth new-variable form with name `ADA_CALENDAR_CREDENTIAL` and its value empty. Return to the Calendar CRM panel for Bryan to replace the key, then have him copy it directly into that Railway value field and click **Add**. The computer-use credential-change rule requires user hand-off for this step. Do not put the key in chat, logs or source files. Verify five staged changes before deploying the complete URL/credential pair together; do not deploy the four-variable draft by itself.
- This checkpoint includes no test booking or email and no API, booking, connection, sync, CRM enforcement or acceptance activation. Continue with protected credential storage, the paused deployment/connection checks and an agreed live pilot.

## Disabled connection created — September 15, 2026

- Bryan clicked **Create disabled connection** in the owner-authenticated Calendar CRM settings and confirmed creation. Read-only hosted verification confirms connection `e154f3c1-35e0-4cb7-9387-b31d14435a4f`, CRM origin `https://alphadogcrm.com`, Auth URL `https://lyopwmiybrhmxbmjjopv.supabase.co`, agency domain `alphadogagency.com`, and `enabled=false`. Do not create a duplicate connection.
- The one-time credential was displayed in the setup tab. It has not been saved to Railway or copied into these records. Preserve that tab until the credential is stored in the CRM server's protected configuration; do not mark the key saved prematurely.
- All 19 client matches remain approved but pending installation. The first pair was entered into the form but not submitted. A fresh read-only query confirms zero saved mappings and zero linked tasks. Chrome subsequently returned a stuck native menu and no screenshot, preventing further owner-authenticated setup; Bryan was asked to bring the existing setup tab back to the front. No direct database writes were substituted for owner authorization.
- Configure `CRM_PUBLIC_ORIGIN=https://alphadogcrm.com`, `ADA_CALENDAR_URL=https://ada-calendar-production.up.railway.app`, and the one-time `ADA_CALENDAR_CREDENTIAL` in the **ADA CRM** Railway service. Keep `ADA_CALENDAR_ENABLED=false` and `ADA_CALENDAR_SYNC_ENABLED=false`. Stage the Calendar URL and credential together and deploy them together: the CRM configuration rejects a partial URL/credential pair. Preserve the seven existing hosting settings.
- Calendar API/booking flags, the disabled connection, and CRM enforcement/acceptance remain unchanged. No live test task or real test email has been created. Resume with the approved mappings and protected credential storage, then the paused connection checks and agreed pilot below.

## Client approval — September 15, 2026

Bryan confirmed every one of the 19 reviewed client pairs, expressly including the United Way and Z Roofing clarification pairs. The exact approved pairs and IDs are recorded in `CRM_CLIENT_MATCH_REVIEW.md` and `CRM_CLIENT_MATCH_PROPOSALS.json`; they remain pending setup, not live mappings. This does not authorize guessed mappings or new clients for the 16 CRM names without a proposed Calendar match. Connection creation was subsequently completed by Bryan as recorded above.

## CRM application release — September 15, 2026

- Bryan accepted the Railway project invitation; the recorded ADA CRM project now opens in his signed-in browser. The service variables page permits adding variables and contains the seven existing settings, including `SUPABASE_SECRET_KEY`, public Supabase configuration and assignment-email settings. No Calendar integration flags, URL or credential were configured. No existing variable was revealed or changed.
- Refreshed the agency repository and confirmed main had not advanced beyond `756aff303937d80b4c0a581bf9088826512d4604`. The working tree was clean, the reviewed integration branch included that main commit, and the release diff passed whitespace validation. The exact same source had already passed the complete local release checks recorded below.
- Fast-forwarded `alphadogagency/ada-crm` main to `5dabe28092d78b0ac1b39155962d6c36eb44446e`. Railway automatically built and successfully activated deployment `b186ae0d-e84a-4a2d-90c5-d16a8dfdc4d2`. **This release did not require a separate Deploy approval click** after Bryan's project access was added; historical notes about the older account setup should not be treated as the current observed result.
- The Railway origin's HTML is byte-identical to the tested local artifact (SHA-256 `68d5f29ee94de7f65a06795caca2a972b491b99dbe59f83aca25bb27a31817d7`). Both CRM HTTPS health endpoints return 200. Anonymous `/api/calendar/status` returns 401; `/calendar-integration.mjs` and `/deploy/calendar-integration.sql` return 404. The signed-in production client list loads with 33 external clients and the two internal companies. My Tasks/Resources interaction and live connected scheduling remain acceptance checks; the earlier full local browser suite is already complete.
- The public `/config.js` contains only its expected public Supabase settings and identifies CRM project `lyopwmiybrhmxbmjjopv`. The disabled connection is prepared for CRM origin `https://alphadogcrm.com`, Auth URL `https://lyopwmiybrhmxbmjjopv.supabase.co`, its published public key, and agency domain `alphadogagency.com`. No Calendar connection credential has been created, stored or enabled yet.
- Read only the two client directories and prepared [client-match review](CRM_CLIENT_MATCH_REVIEW.md). No matches or new clients were written. Browser setup still needs the explicit credential-creation confirmation required by the computer-use tool, followed by owner confirmation of client matches and an agreed live pilot. A browser action that changes credentials must follow the tool's hand-off rules where applicable; never bypass the owner-authenticated Calendar setup route.
- No live fixture task or real test email was created. Neither hosting variables nor the permanent CRM enforcement/acceptance switches were changed. Calendar's unrelated working changes in `docs/RESUME.md` and `next-env.d.ts` remain outside these release records.

## CRM database release — September 15, 2026

- Bryan accepted the Alpha Dog Agency Supabase organization invitation. The existing CLI login now successfully queries the intended CRM project, `lyopwmiybrhmxbmjjopv`. No database or ownership transfer was needed.
- Read-only inspection confirmed the existing task identity/assigner, My Day and internal-company schema. The live assignment and daily-email function bodies matched their reviewed pre-integration SQL exactly. GitHub still reports CRM main at `756aff303937d80b4c0a581bf9088826512d4604` and the tested integration branch at `5dabe28092d78b0ac1b39155962d6c36eb44446e`; no intervening code changes required another local test run.
- Applied only `deploy/calendar-integration.sql`, followed by `deploy/calendar-sync.sql`, to that CRM database through the authenticated Supabase SQL command. Both transactions succeeded. Do not rerun these one-time migrations or the original schema/seed scripts.
- Post-install read-only verification matched all 18 installed integration function bodies to the reviewed files. None is executable by `anon` or `authenticated`. All three private integration tables have RLS enabled and deny browser reads. Connection enforcement and acceptance remain false, its identity is null, the sync cursor is 0 with no lease, and there are no submissions or linked tasks.
- Before/after counts remain 35 clients, 29 tasks, 2 resources, 36 assignment-email records and 5 daily-email records. No live fixture tasks were created, no worker was invoked for testing, and no real test email was sent. Both applications' public health endpoints and the CRM sign-in HTML return 200; Calendar's integration status still returns the expected 503 `crm_disabled`.
- Railway access is the next prerequisite: the signed-in Bryan account's workspace selector lists only Bryan Arambula's Projects, and the recorded CRM project URL returns 404. The CRM app has not been merged to main or deployed by this checkpoint. Ask the owner to invite Bryan's existing Railway account to the **ADA CRM project** with **Editor** access, or have the owner manage its connection variables and deployment directly. [Railway project invitations](https://docs.railway.com/projects#inviting-members) and [project roles](https://docs.railway.com/projects/project-members) describe that access. Recheck the actual deployment approval behavior after access is granted; do not assume the old approval requirement is unchanged.
- Resume with CRM hosting settings and release verification, then connection credentials, owner-confirmed client mappings and the controlled activation checks below. The complete integration remains off. No GitHub push, Railway deployment or hosting-variable change was performed at this checkpoint.

## Calendar release — September 14, 2026

- Calendar main is now `1a75bb30313a0f812e69b3021ecbe9c8f9fa2bd8`. Before main advanced, [GitHub run 34910322190](https://github.com/bryguy-404/ADA-Calendar/actions/runs/34910322190) passed lint, type checking, 1,385 tests in 72 files, the production build, and all 183 browser scenarios. The later main push reuses this same verified source commit.
- Railway deployment `b5505fcc-9494-463f-87c7-352e58bccde8` reports success. The Supabase GitHub deployment also reports success; independent hosted SQL confirms all four migrations `202609140001`–`202609140004` were applied through that integration. No CLI migration push was used.
- Live HTTPS checks pass: `/api/health` 200, password sign-in page 200, anonymous `/api/state` 401. The new `/api/integrations/crm/v1/status` returns the expected 503 `crm_disabled`. Working hours remain 09:00–17:00 and the saved reserve remains 0. No CRM credential, mapping, booking, or pilot task was created.
- CRM's reviewed merge is `5dabe28092d78b0ac1b39155962d6c36eb44446e`, published to `alphadogagency/ada-crm` on `codex/calendar-integration`. CRM main stays at `756aff3`; its integration schema and application have not been released. The next step is access to the existing agency Supabase project, followed by the two reviewed CRM migrations and the paused CRM deployment. The complete integration is not active yet.
- No real test email was sent. Local fixture servers and the disposable CRM Supabase stack were stopped; the existing Calendar stack and unrelated local projects were preserved. Unrelated working changes in `docs/RESUME.md` and `next-env.d.ts` remain untouched.

## September 14 prerelease review

- Calendar's current canonical GitHub repository is `bryguy-404/ADA-Calendar` (the older `tito3288` references are historical). Hosted main remains `04f3765`. Railway's active deployment is `5234a683-084c-4397-bc37-253b023645dd`; the latest GitHub Supabase check succeeded. Read-only hosted SQL confirms all 15 existing migrations through `202609090006`, with the four integration migrations still pending. Saved hours remain 09:00–17:00, Indianapolis timezone, reserve 0. Neither new integration environment flag is set, so both are disabled.
- CRM main advanced to `756aff3` with internal company tasks. Its recorded Railway deployment `580f9aaf-f017-4bb7-8dd6-74284b0bdf6a` reports success. The integration branch incorporates that change, retaining internal company visibility without revenue/client editors, Calendar client mapping, and locked linked tasks. Verify whether its `internal-companies.sql` has been applied before the CRM database release; do not reseed an existing production database.
- The combined CRM passes its build/syntax check, 78 tests, rollback-only SQL checks (including internal companies), seven browser scenarios, and a Node 24 Docker build. The full real two-app suite passes again, including unmapped internal task rejection, direct-write protection, owner-confirmed mapping and a browser booking that appears once in both databases. These are local fixtures and captured notifications only.
- Calendar main had five existing browser CI failures. Corrected tests account for elapsed time, the server-seeded date versus the browser's fixed fixture date, hydration before the controlled demo-role selection, and cross-week drag events. All 32 affected browser scenarios pass; lint/typecheck pass. No Calendar application or scheduler behavior changed in these corrections.
- The current Supabase login can access Calendar but receives a 403 on CRM project `lyopwmiybrhmxbmjjopv`. CRM schema installation awaits an invitation for Bryan's existing Supabase account or installation by the CRM's authorized database owner. This requires collaboration access to the existing database, not a database transfer, another hosted database, or sharing the owner's password. CRM production has not been modified by this review.

## Review and release order

1. Review both integration diffs against the latest main branches: Calendar `codex/crm-integration-foundation`, CRM `codex/calendar-integration`. Before merging, fetch both repositories and reconcile any intervening changes; rerun affected checks. Confirm the release commits, database destinations and current deployment settings. Preserve unrelated Calendar changes in `docs/RESUME.md` and `next-env.d.ts`.
2. Keep Calendar's `ADA_CRM_INTEGRATION_ENABLED=false` and `ADA_CRM_BOOKING_ENABLED=false`. Keep CRM's `ADA_CALENDAR_ENABLED=false` and `ADA_CALENDAR_SYNC_ENABLED=false`; its new connection row installs with enforcement and acceptance false. Preserve existing email configuration until the controlled test window is agreed.
3. Release Calendar first. Its existing Supabase GitHub integration is the recorded migration deployment path, while Railway builds the application. Apply the four ordered CRM migrations `202609140001`–`202609140004` through that path, then verify hosted migration history and successful Railway deployment. Do not also run a competing CLI migration push. A passing GitHub CI run does not establish migration success.
4. Release the CRM schema and code after Calendar is ready. On the existing CRM database, confirm the earlier task identity/assigner and My Day migrations are present. Apply **only** the new `deploy/calendar-integration.sql`, followed by `deploy/calendar-sync.sql`, using CRM's existing reviewed SQL process. The latter requires the existing `my-day-emails.sql`. These are one-time migrations; never rerun the original schema/seed or overwrite the integration functions with an older notification script.
5. Merge/push the reviewed CRM code and complete the agency's Railway deployment approval if its current account configuration requires it. Verify the new production image and `/healthz`, sign-in, existing task behavior and Resources/My Tasks. The recorded agency setup requires the Railway owner to click Deploy; check the actual pending deployment rather than inferring success from a GitHub push.

The reviewed Docker images use Node 24. The CRM image must include the Calendar server modules and source helpers in its `.dockerignore` allowlist. Both builds exclude test fixtures and private environment files from the shipped runtime.

## Connect while new assignments remain paused

- In Calendar's owner-only CRM settings, create the disabled connection using the real CRM HTTPS origin, canonical Supabase Auth project URL, public/publishable key and agency domain. Copy the one-time connection credential directly into CRM's server-only Railway variables. A lost credential is replaced through Rotate key; do not create a second connection with a fresh identity/cursor to bypass recovery.
- CRM needs its existing public Supabase settings plus `SUPABASE_SECRET_KEY` or its service-role fallback, `CRM_PUBLIC_ORIGIN`, `ADA_CALENDAR_URL`, and `ADA_CALENDAR_CREDENTIAL`. Private keys must not appear in `/config.js`, source control, task JSON, logs or browser storage. Browser sessions retain only their normal user sign-in token.
- Use each CRM client's Copy Calendar client ID button or the authenticated paginated client directory. In Calendar, select and confirm the actual matching Calendar client. Review all clients involved in the pilot; a name/alias suggestion alone never authorizes a booking.
- Enable the Calendar API flag and its owner-controlled connection while leaving booking disabled. Confirm authenticated connection/availability behavior and that anonymous/wrong-credential requests fail. Verify the displayed timezone and saved zero-minute interruption reserve without changing them.
- At the announced activation window, set CRM enforcement true with acceptance false, then enable its sync flag. This starts the permanent protection for new Bryan assignments; acceptance stays paused until the next checks pass. Verify the connection UUID is pinned, the cursor catches up, the lease is released, and health is current. Enforcement cannot be turned off afterward. Existing grandfathered tasks remain editable.

## Controlled activation and acceptance checks

With both approved deployments verified, enable Calendar booking, CRM's assignment flag and CRM acceptance. Use agreed test tasks/users and captured or explicitly allowlisted notifications. Do not send test mail to other teammates. A live test of an assignment is a real calendar mutation and should use an agreed disposable task/open slot.

Verify a clean-fit assignment appears once in both applications with the same sessions; a conflict offers alternatives and stays unbooked pending owner approval; only the original requester can reply; protected time requires an explicit owner override; and owner edits/completion/Undo return to CRM. Confirm legacy work still behaves normally, mappings cannot be bypassed, no duplicate assignment email is queued, and My Day includes scheduled work rather than approval requests. Confirm a CRM restart catches up without the requester staying signed in.

Record the actual commits/deployment IDs, migration versions, connection identity (never the secret), pilot task IDs, observed cursor/last success, notification results and the person authorizing normal delivery. Local verification is complete; hosted DNS/TLS, credentials, deployment hooks and real delivery remain acceptance checks at release time.

## Pause and recover

**Stop new assignments:** set CRM acceptance false and/or its assignment flag false; Calendar booking can also be paused. Keep CRM enforcement, source records, credentials and sync enabled so existing work continues to update. Do not roll back to a CRM build that offers unchecked assignments after enforcement has been activated.

**Broken connection or compromised key:** rotate the Calendar credential and replace the CRM server value, preserving the same connection UUID/cursor. Disable the Calendar connection if immediate revocation is needed; its change history continues accumulating for later catch-up. A two-minute stale warning is expected while reads are unavailable. Recover the original durable operations; never clear submissions or invent new IDs to bypass an uncertain outcome.

**Application defect:** pause acceptance first, then deploy a compatible fix or previously verified integration build. Retain the additive schema. Database correction uses a reviewed forward migration or the established backup/restore procedure, never a production reset. A pre-integration build is not a safe bypass once enforcement is active. Do not delete links, closed-operation records or cursor history, or automatically recreate an owner-undone booking.

## Local evidence and reproduction

`npm run test:crm-fullstack -- /absolute/path/to/ada-crm-calendar-integration` runs both applications with real, separate local Auth/SQL services. Prerequisites are the project's locked npm dependencies, CRM's locked dependencies, Docker, Python, Chrome, the existing ADA local Supabase stack on 5542x, and `agent-browser` on PATH (or `ADA_AGENT_BROWSER_BIN` pointing to its installed executable).

The harness creates/resets only the disposable `ada-crm-integration-test` stack on 5552x, whose configuration/migration copies live under `/private/tmp/ada-crm-fullstack-local`. It never resets the existing Calendar database or another project's stack. Calendar fixtures use random IDs and are removed afterward. CRM fixture data remains in its separate disposable stack until the next reset; it can be stopped using the CLI with that exact workdir.

Test-only Node/browser transport maps synthetic HTTPS service addresses to loopback, preserving the production origin/Auth validation rules. Auth, REST, realtime, scheduling and database functions execute normally. Browser tests serve the installed Supabase SDK locally. No hosted request, sign-in email, AI request or real notification delivery is used. The test transport is outside the production runtime and has no production enable switch. Fault injection drops an actual completed response or blocks one dispatch to verify recovery.

Screenshots and private app logs are under ignored `test-results/crm-fullstack`; the CLI's private local status log is in the temporary stack directory. Never publish logs containing credentials. The suite verifies browser booking in both systems, due dates, conflicts/alternatives, requester/owner boundaries, live realtime updates, approval/decline, protected overrides, private time, completion/Undo/cancellation, concurrency/staleness, lost acknowledgements, settlement, restart/catch-up, rotation/revocation, source retention and queued-only notification ownership.
